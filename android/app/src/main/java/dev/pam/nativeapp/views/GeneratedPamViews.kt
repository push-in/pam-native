package dev.pam.nativeapp.views

import android.content.Context

/**
 * Replaced by `pam mobile codegen` when a project declares native views.
 */
object GeneratedPamViews {
    fun create(context: Context): Map<String, NativeViewFactory> {
        context.applicationContext
        return emptyMap()
    }
}
