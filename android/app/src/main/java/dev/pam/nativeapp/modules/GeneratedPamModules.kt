package dev.pam.nativeapp.modules

import android.content.Context

/**
 * Replaced by `pam mobile codegen` when a project declares native modules.
 */
object GeneratedPamModules {
    fun create(context: Context): Map<String, NativeModule> {
        context.applicationContext
        return emptyMap()
    }
}

